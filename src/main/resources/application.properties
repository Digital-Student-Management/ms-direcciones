spring.application.name=ms-direcciones

# Configuración del servidor (Puerto único para este microservicio)
server.port=8082

# Configuración de la Base de Datos MySQL
# (createDatabaseIfNotExist=true creará la base de datos automáticamente en tu Workbench)
spring.datasource.url=jdbc:mysql://localhost:3306/ms_direcciones_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=fullstack3-2026

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true